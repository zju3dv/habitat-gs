"""habitat_agent.prompts — prompt loader and builder.

Phase 1 moves `PromptBuilder` into `legacy_builder.py` as-is.
Phase 4 will replace it with a versioned `PromptSpec` / `PromptLibrary` system.
"""
