"""habitat_agent.agents — concrete agent implementations.

Modules:
- `nav_agent`       : NavAgent (autonomous navigation subagent)
- `nav_agent_main`  : subprocess entry point for NavAgent
- `chat_agent`      : ChatAgent (interactive chat + tool use)
"""
